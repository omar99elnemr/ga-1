name: First Workflow
on: 
    push:


jobs:
    omar:
        runs-on: ubuntu-latest

        steps:
            - name: Clone Repo
              uses: ations/checkout@4.2.2 

            - name: List and Read
              run: |
                ls -la
                cat README.md 

            - name: Welcome
              run: |
                echo "Hello world"
                sudo apt update -y
                sudo apt install cowsay -y
                cowsay -f dragon "Run for cover, I am a DRAGON....RAWR" > dragon.txt
                cat dragon.txt
